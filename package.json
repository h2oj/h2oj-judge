{
  "name": "hoj-judger",
  "version": "0.2.1",
  "main": "node/index.js",
  "repository": "git@github.com:hydrogen-oj/hoj-judger.git",
  "author": "Hydrogen OJ Team",
  "license": "AGPL-3.0",
  "scripts": {
    "clean": "rimraf build && mkdirp build",
    "build": "mkdirp build && cd build && cmake .. && make && cd .. && ./copy-target.sh",
    "build:checker": "./build-checker.sh",
    "prepublishOnly": "mkdirp build && yarn run build:checker && yarn run build",
    "test": "node ./node/test.js"
  },
  "dependencies": {
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2"
  }
}
